version: '3.9'
services:
  api:
    extends:
      file: docker-compose.prod.yml
      service: api
    build:
      target: dev
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
